{:paths ["resources" "src"]
 ;; No dependencies on purpose. See
 ;; plumcp.core.server.oauth-as.internal for why this module does not take
 ;; module/core: a relative :local/root inside a git checkout is unresolvable
 ;; by older tools.deps versions, which would stop Leiningen-based consumers
 ;; from using this module as a git dep at all.
 :deps {}}
